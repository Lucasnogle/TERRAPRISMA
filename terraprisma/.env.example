# ============================================================
# TERRAPRISMA Backend — Environment Variables
# ============================================================
# Copy this file to .env and fill in the values.

# Server
PORT=3000
NODE_ENV=development

# JWT Authentication
# IMPORTANT: Use a strong, random secret in production (min 32 chars)
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=1d

# CORS — Comma-separated list of allowed frontend origins
# MUST include scheme (http:// or https://)
# Origin = scheme + host + port → ex: http://localhost:5173
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
# Production example:
# CORS_ALLOWED_ORIGINS=https://app.seudominio.com,https://www.app.seudominio.com

# Trust Proxy — Number of proxy hops in front of Express
# Nginx = 1 | Cloudflare + Nginx = 2 | Direct = 0
TRUST_PROXY_HOPS=1

# Firebase Admin SDK
# Option 1: Place serviceAccountKey.json in /secrets/ folder (preferred)
# Option 2: Set these env vars (fallback)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----\n"
